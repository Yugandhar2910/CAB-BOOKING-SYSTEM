export interface Driver {
  driverId: number;
  name: string;
  email: string;
  licenseNumber: string;
  phoneNumber: string;
  vehicleType: string;
  vehicleNumber: string;
  available: boolean;
  verified: boolean;
}
